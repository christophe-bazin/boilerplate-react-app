/**
 * Reset Password page
 * Client component that renders the ResetPassword form
 */

'use client';

import ResetPassword from '../../../src/components/auth/ResetPassword';

export default function ResetPasswordPage() {
  return <ResetPassword />;
}
